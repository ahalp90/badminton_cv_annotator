# C1 corrections

Current HEAD was `872ea806c5b69a195f32e9f60b5840294eefb6cd`; the worktree was clean before this task. No numerical rerun or new experiment was performed.

1. **GX temporal directions.** Checked `CLAUDE_FOLLOWUPS/gx_directions/{run_gx_directions.py,note.md}` and the follow-up evaluation/status. `set_bound` reports an incidence lower bound, not an attained fit. The three later rows with values below 3.5 px are now described as lower bounds relative to the GX0 control. Attainable fits and generated courts were not measured. Raw homogeneous-vector angles are chart-dependent descriptions, and camera equivalence remains unverified. The seven cached checks did not test frames 30/60/90 or modern independent-versus-shared scoring.

2. **Distortion.** Checked both distortion notes and their failed gates. The observed GX sagittas remain recorded as noisy measurements. The summaries now say the methods are inconclusive and establish neither a validated GX-specific measurement nor an upper bound. Further work is deferred for priority reasons. No third study was started.

3. **Temporal scoring.** Checked `camera_calibration_assessment.md` and `CLAUDE_FOLLOWUPS/SCHEDULE.md`. The decisive open test is independent versus shared scoring on the same automatic candidate union within a verified view. The old Yellow floor-score dominance certificate is explicitly limited to its saved matrix and does not establish modern Amateur-2 line/paint dominance. The audit is deferred and unexecuted. Its historical sampling was not replaced with distant cached successes. A small cached-frame pilot remains a separate option; a full stability/reuse framework is out of scope.

4. **Identifiability.** Checked `identity_diagnostics/note.md` and the WebUI identity assessment. Rank and spacing remain descriptive warnings conditional on named-line correspondence. Rank eight is not court identity, sub-pixel spacing is not a universal threshold, and cropped or foreshortened real courts may also be ambiguous. No implemented or validated free rejection/abstention gate is claimed.

5. **GX rows and allocation.** Checked `gx0_rows/note.md`, the person-mask replay and the current evaluation. The wall seam, floor strip and face remain the session's visual attributions. Both person rules remove the face, but only one improves the fit, so the face's effect and greedy allocation causality are not isolated. No new ablation was run.

6. **Caps and proxy scope.** Checked `line_identity/{results.md,evidence.md,worklog.md}` and `run_automatic.generate/evaluate_pool`. The local one-pair GX0 proxy improves 34.3→12.6 px, while the reported full-pool nearest worsens 7.7→9.0 px. Filtered observations reach downstream scoring as well as axis matching. The recorded Amateur-3 per-pair loss is about 4.07 px, but it is not the dominant roughly 40 px gap. Detailed raw-trace verification remains for C2.

Remaining material question: whether modern cross-frame shared scoring improves on the same-pool independent comparator while preserving Amateur-3. The repository now leaves that route open instead of treating it as disproved.
