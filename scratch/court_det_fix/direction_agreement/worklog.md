# Direction-agreement experiments: worklog

## Resume

- Stage: E4 running. The GX0/M smoke passed (exit 0, four exact replays). The other 17 case-arms run as four single-thread streams on the remote.
- Next action: poll `bash sync.sh status` until `e4_M_a`, `e4_M_b`, `e4_R_a` and `e4_R_b` all have exit receipts of 0 (expected about two hours from 16:11). Then `bash sync.sh launch diag diagnose_matrix`, pull, `summarise.py`, `render_gallery.py`, results.md, audit 3, report loop.
- Active jobs: labels `e4_M_a` (GX5, Am2-150), `e4_M_b` (Am2-28019, Am3-0, four ShuttleSet), `e4_R_a` (GX0, GX5), `e4_R_b` (three amateur, four ShuttleSet); PIDs in `runs/<run>/receipts/<label>.pid`. Second Opus audit (E3 code and results, E4 stage code, smoke outcome) runs locally in the background.
- Last passed gate: E4 smoke exit 0 with exact final-evaluation and selection replays at both rescoring stages.
- Blockers: none.
- Paths: local `scratch/court_det_fix/direction_agreement/`; remote mirror `R/direction_agreement/` (R is the packet's remote root); run name in `run_name.txt`; outputs under `runs/<run>/`.

## Scope and overrides

The packet is `scratch/court_det_fix/worklog/CLAUDE_DIRECTION_EXPERIMENTS.md`. The user's session instructions override it where they clash:

- Session model is Claude Fable 5.1, not Opus 5 xhigh. The manifest records the actual model.
- Local commits on `fix/court-det` are allowed. Audits by Opus 5 (medium) run at stage checkpoints via the CLI, read-only.
- Local outputs live in this tracked folder, not under the earlier 20260914 tree. The remote outputs live in the mirror folder, not under `automatic_axes_20260914/<run>`.
- The packet's provider-launch metadata folder was for a Codex-to-Claude handoff and is not used.
- Committed files use the aliases `R` (remote root) and `L` (local source tree at `scratch/court_det_fix/worklog/checks/independent/player_guided/20260914`). The gitignored `paths.local.sh` maps them to real locations.

## Log

- 2026-09-15 14:49. Folder, `.gitignore`, alias file and run name created. Read the packet, the shared modules it instruments (`detector._merge_lines`, `assignment.prepare_observations`, `vp_pruning.retain_pencils`), the matcher entry points (`run_automatic.generate`, `rescore_camera_pool.rescore`), the SVD helpers and the previous gallery renderer.
- 2026-09-15 14:52. All 43 shared modules and every saved input are byte-identical between `L` and `R` (MD5). The remote has no running jobs. Baseline checkpoint `9fcec94`; HEAD `d0c9a12` differs only by documentation.
- 2026-09-15 14:55-15:02. Wrote `common.py`, `membership.py`, `selection.py`, `run_arms.py`, `run_fits.py`, `run_matcher.py`, `diagnose_matrix.py`, `manifest.py`, `run_remote.sh`, `sync.sh` and three test modules. 30 synthetic tests pass; Ruff clean after auto-fix (import order, one unused import, two redundant casts, `datetime.UTC`).
- 2026-09-15 15:03. First `run_arms` launch failed at import: `ARMS` was imported from `selection` instead of `common`. The synthetic tests never imported the stage scripts. Added `tests/test_imports.py` (every module must import), fixed the import, relaunched. 38 tests pass.
- 2026-09-15 15:07. E0-E2 complete for all nine cases in 53 s, exit 0. Every E0 assertion held: the instrumented merger reproduces `detector._merge_lines` exactly, the estimator replay matches the saved record field by field, the foot anchor reproduces `angular_residuals` bit for bit, and the bucket instrumentation leaves the selection unchanged. Two GX cases have only 106 and 108 merged lines and SS03-19 has 124, so "128 direction lines" is a cap, not a count.
- 2026-09-15 15:11. E3 complete in 43 s, exit 0. 72 sets, 17,280 ordered-pair fits, none non-finite, 4 non-converged. The three `svd_fixed` replays (GX0, GX5, Am2-28019) differ by 0.0 px from the saved records. Best control fits are mixed between arms; see `runs/<run>/summary.md` and hypotheses.md.
- 2026-09-15 15:14. Manifest had recorded OpenCV at 32 threads because `manifest.py` did not pin threads (the stage scripts do); pinned it. The root `.gitignore` ignores every `runs/` directory, so the folder's ignore file now re-includes `runs/` before excluding the matrices; the checkpoint commit was amended to carry the run records.
- 2026-09-15 15:13. First Opus 5 (medium) read-only audit launched over E0-E2 code, originals and an auto-extracted evidence digest. Wrote `summarise.py`, which builds `runs/<run>/summary.{json.gz,md}` from the stage records.
- 2026-09-15 15:18. Dry-ran `diagnose_matrix` on the baseline records alone (label `diag_dry`, exit 0) to exercise the accounting before the matcher stage; its outputs were deleted afterwards so they cannot pass for the final matrix.
- 2026-09-15 15:18. Audit 1 returned (about five minutes). Verdict: proceed with fixes. Blocker: the packet's E0 step 5 global-selection replay (`check_pool_replays.py`) had not been run. Should-fix: `run_arms --resume` could never skip (hash dict mismatch); the E4 resume check ignored input identity; missing E4 stages produced quiet CSV rows; accounting rows lacked the run and selection-stage keys. Notes: some E2 cross-arm asserts hold by construction and are not evidence; the manifest thread count; the E1 column label assumed 128 lines; masks are derivable from the matrices rather than stored. It also suggested band-restricted residual statistics and an angular (not ID-based) comparison of M's directions with B's. All findings on A-F confirmed the instrumentation faithful to the packet.
- 2026-09-15 15:21. Ran the global-selection replay on the remote: nine exact messages, exit 0. Checked that no directory on the remote path shadows an experiment module, and moved this folder to the front of `PYTHONPATH` anyway; unset `PYTHONOPTIMIZE` so replay asserts cannot be stripped. Applied every should-fix and note: resume identity in both scripts, `--allow-missing` gate in the accounting, `run` and `selection_stage` columns, thread requested/reported fields, dynamic column label, band-restricted change statistics, and per-direction nearest-baseline angles for M, R and MR. Removed the by-construction asserts from the E0 evidence list. Gates: 38 tests, Ruff, scoped Pyrefly, shell syntax all exit 0.
- 2026-09-15 15:24. Regenerated manifest, E0-E2 and E3 under the fixed code (exit 0 each). Every number matches the first run; OpenCV now reports one thread after the pin. On GX0, 12 of M's 16 directions lie within 0.7 degrees of some B direction and two lie more than 10 degrees from any; all 16 of R's lie within 2 degrees, as expected from shared buckets.
- 2026-09-15 15:26. Launched the E4 smoke: GX0 under arm M. Committed the audit fixes as `0caae0a` at 15:29 and refreshed the manifest against that commit (dirty count 0). Times in this log are AEST; remote receipts are UTC (AEST minus 10 hours).
- 2026-09-15 16:08. Smoke passed: exit 0, generation 2145 s, both rescoring replays exact, 256 final and 3,265 all-camera entries. Launched the remaining 17 case-arms as four single-thread streams (16:11 and 16:16; the first launch helper stalled, see runs.md). Launched audit 2 (E3 results and code, E4 stage code, smoke outcome).
- 2026-09-15 16:17. Ran a partial accounting for GX0 (B and M) and rendered the gallery on it locally: five panels, layers and labels correct. GX0/M is a clear regression: both M winners lie over 300 working px from the control against 7.7 px for B's paint winner, matching E3's direction-fit potential (19.7 against 6.8).
- 2026-09-15 16:20. Audit 2 returned (about four minutes). Verdict: continue E4, then accounting and report with fixes. Should-fix: accounting could KeyError on a pair whose basis failed (fixed with a `basis_failed` count); the gallery collapsed shared courts under the first arm's ID and CSS class (fixed: one ID per label, one class per arm, a court shows while any of its arms is checked); three prose figures disagreed with the fact packet (M shares 0-5 leaders, SVD helps B on eight views, M is 0.02 px worse on SS03-19; fixed); best-converged fits unreported (the best finite pair converged in all 72 sets; stated); do not push code before E4 finishes (respected); the partial accounting files were stale (deleted; `summarise.py` now refuses an incomplete matrix unless told otherwise); "not visually judged" on B winners ignored the user's earlier rulings (the gallery now joins `automatic_axes_visual_judgements.md` by candidate ID). Notes recorded in evidence.md and results.md: in-memory rescoring replay, identity reuse never triggered, resume identity scope, the floor gate is -1 for every entry in both GX0 pools (inherited from the baseline), E4 timings are concurrent.
