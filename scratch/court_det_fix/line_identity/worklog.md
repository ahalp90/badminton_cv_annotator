# Line identity: worklog

## Resume

- Stage: matcher run. The local replays are done (courts before the gate, axis replay, paint profiles, filter replay) and audited twice; the compute-host matcher run on four arms is the next step, then accounting, then the report.
- Active jobs: none on the compute host until the launch logged below.
- Last passed gate: filter replay reproduces every saved direction record before filtering (nine views); the accounting core reproduces the direction experiment's own row for GX0 under M; five synthetic tests pass.
- Paths: local `scratch/court_det_fix/line_identity/`; the earlier follow-up checks live in the gitignored `scratch/court_det_fix/worklog/webui_evaluation_returns_15092026/CLAUDE_FOLLOWUPS/`; remote mirror `R/line_identity/` once matcher runs start (R is the remote experiment root, mapped in the gitignored `paths.local.sh`).

## Terms used in this folder

- Direction selection: the automatic choice of 16 vanishing directions from merged line groups (`vp_pruning.estimate`, coverage rule). The baseline selection is arm B; the direction experiment's two changes are the midpoint-anchor selection (M) and the precise-representative selection (R).
- Direction fit: the least-squares court fitted to the frozen control from one ordered pair of selected directions with both vanishing points fixed (stage E3 of the direction experiment). It says what the directions could support; it is not a generated court.
- Axis matching: how the matcher turns one direction pair into courts (`projective_seed.match_axis`): enumerate a scale and offset from every pair of direction-compatible observed line groups against every pair of court markings, keep enumerations with at least three supported markings that the necessary player rule accepts, drop duplicates by marking assignment, keep the 512 best by score (the per-direction cap), then combine every kept horizontal matching with every kept vertical matching.
- Kept matching: one of the 512 survivors per direction. Combined court: one horizontal kept matching paired with one vertical one. Ranks in this folder are 0-based positions in the score order; the cap keeps positions 0 to 511. The court template is symmetric, so every physical matching is enumerated twice (once per orientation) with the same support; a rank counts both twins.
- Incidence bound: the lower bound on the maximum corner error of any court that keeps one ordered pair of the selected directions (`geometry_certificate.set_bound`, from the WebUI direction audit). A fitted court can do no better than it.
- Control: the frozen reference court per view from the direction experiment (`evidence.md` there); six visually approved, three manual references. Distances are maximum corner distance in working pixels (960 by 540) with the 180-degree relabelling allowed.

## Log

- 2026-09-16 07:15 (times in this log are AEST from file and commit timestamps). Read the handover from the follow-up checks. Both detached jobs of the courts-before-the-gate run had exited 0 on the compute host (GX0 under M 35.9 min, Amateur-3 under R 20.9 min). Pulled the records (530 MB, gitignored cruft in the follow-ups folder).
- 2026-09-16 07:44 to 07:46. Gate 1 passed: both new generation records equal the saved ones in every pair status, shortlist, winner and pooled count. Gate 2 passed after one script fix: the earlier cross-check compared an index into the post-mask array with an index into the pre-mask array; it now maps through the mask. Result: the nearest court before the masks is the nearest usable court on both views (33.8 px GX0, 40.1 px Amateur-3); nothing within 20 px among 22 and 17 million combined courts. Note, table and evaluation updated in the follow-ups folder.
- 2026-09-16 07:50. Read `match_axis`: each combined court is the product of 512 kept matchings per direction, and on the best-fit pairs the per-direction cap excluded 1,236 to 25,852 distinct matchings per direction (the record's `distinct_assignments` minus 512). Wrote `axis_replay.py` to separate enumeration, the support and player rules, duplicate removal and the cap, using the direction-fit homography decomposed in the pair's basis as the ideal per-direction scale and offset.
- 2026-09-16 08:00. Axis replay on six pairs (GX0 under M pair 23; Amateur-3 under R pairs 43 and 30; Amateur-3 under B pair 43; GX0 under B pairs 22 and 143). Gates: the 512 kept matchings per direction equal the record by ID and parameters, the enumerated and distinct counts equal the record's diagnostics, and the kept-by-kept nearest court equals the record's nearest for the pair (33.822, 46.100, 40.140, 4.271, 7.662 and 34.327 px). Finding: on Amateur-3 under R pair 43 a horizontal matching that reaches the fit (4.28 px with the vertical ideal, score 0.658, 5 of 5 markings) is enumerated and passes the support and player rules, but the duplicate removal keeps a different matching with the same marking assignment (6.24 px, score 0.581), and that one sits at position 11,518 of 23,864 in the score order; all 512 kept horizontal matchings support 5 of 5 markings with scores above 0.885. The vertical side survives (position 103). Under B the same pair keeps a 5.4 px horizontal matching at position 367. Viewing the native frame, the session judged the many groups along the sideline direction (82 direction-compatible) to be floor-plank seams; nothing in the records attributes them.
- 2026-09-16 08:05. Reran the replay with the uncapped combination: every distinct horizontal matching with every distinct vertical one gives 6.19 px on Amateur-3 R pair 43, 8.73 on pair 30, 6.40 on GX0 B pair 143 and 23.24 on GX0 M pair 23, against 46.1, 40.1, 34.3 and 33.8 from the kept matchings.
- 2026-09-16 08:07 to 08:19. Paint-profile study on all nine views (`paint_profiles.py`), three versions of the contrast measure. The first used one flank window at 6 to 10 working px with the flank mean over both sides; the second took each side's mean separately and measured against the brighter one, after the first had passed GX0's tan floor strip (a step, not a ridge); the third added a nearer window (3 to 6 working px) and kept the larger contrast, after a smoke replay of the filter on GX0 (not kept in `runs/`) had dropped 13 of 17 outline fragments and lost the y-direction (bound 6.0 to 32.6) with the second version. All three changes were made after reading GX0 results, so GX0 is the view the measure was shaped on; the thresholds (contrast 20, saturation 90) were then read off the study's quantiles, which use control-derived labels on the same nine views. No view is held out. Committed at 08:11 (`771578b`) with the axis replay.
- 2026-09-16 08:17. Audit 1 (Opus 5, medium, read-only) returned: proceed with fixes. Should-fix items and their fixes: gate 4 of the axis replay was printed but not asserted and was compared with the whole record's nearest rather than the pair's (now asserted per pair against the pre-mask table for M and R and bounded by the pair's shortlist for B); the worklog misread the pair-43 ladder (corrected above); the excluded-matchings range was wrong (corrected); three causal readings were presented as established (now marked as the session's visual judgement or as inference); the pre-gate note and the follow-ups evaluation still said the axis matching holds no close court, which the uncapped combination contradicts (addenda written); the study's "edge" label covered only the outline (now every painted marking); saturation results were omitted (now reported; on the GX views the marking fragments are more saturated than the rest, 59 and 51 against 46 and 48 at the median, so the saturation term does no work there); the committed pre-gate scripts need the original records folder (documented). Notes taken: ranks are 0-based; mirror twins; the chart-leak figure is not evidence; two pre-gate checks are circular; terms defined above; `status.md` refreshed; tests added.
- 2026-09-16 08:20 to 08:24. Wrote `filter_replay.py`: three fragment filters fixed before the nine-view run (person-box midpoint rule; paint rule at contrast 20 and saturation 90, the thresholds chosen from the study's quantiles so that GX0's six offending fragments, at most 16, fall below and the marking medians, 21 and up, sit above; both together) plus two contrast sensitivities (15 and 25). Stage 1 replays the unchanged merge and coverage selection on the filtered fragments and measures the incidence bound, the best direction fit and the angles to the control's vanishing points; stage 2 reruns the axis matching for each arm's best-fit pair on the filtered observations with no cap, and for one extra arm keeps the baseline directions and filters only the observations (`paint_observations_only`). Fixes from audit 1 committed at 08:24 (`57bc118`). A first nine-view run finished at 08:22 with the second contrast version; the rerun with the marking label and the `paint_observations` matcher inputs finished at 08:29. The matcher inputs (filtered pack entries and direction records) are written under `inputs/<arm>/`; `run_matcher.py`, `run_remote.sh`, `sync.sh` and `account.py` were written and the folder pushed to the compute host, where the driver's imports resolve to the frozen helpers and the legacy zone module.
- 2026-09-16 08:29. Filter replay complete on nine views (exit 0, `runs/filter_replay/`). At the direction stage no arm improves every view: the person-box rule takes GX0 to 1.2 px bound and 1.5 px fit (from 6.0 and 6.8) and GX5 to 3.1 and 11.5 (from 9.7 and 23.8) but moves Am2-150 from 2.9 and 3.3 to 6.1 and 8.9 and three ShuttleSet views from 0.9 to 2.3 out to 4.2 to 5.1; the paint rule regresses GX0 to 32.8 and SS21-20 to 12.3 while improving GX5 and Amateur-2 frame 28019; the contrast sensitivities are not monotone on Amateur-2 frame 28019, Amateur-3 and SS21-20. At the axis stage, keeping the baseline directions and filtering only the observations helps where the cap had cut a near-fit matching (GX0 pair 143's horizontal from position 1459 to 181, kept-by-kept 34.3 to 12.6; Amateur-2 frame 28019 kept-by-kept 61.6 to 7.6), leaves the four ShuttleSet views and Amateur-2 frame 150 within 2.1 px, and costs about 2 px on Amateur-3 (4.3 to 6.2, where the baseline had not been cut) and GX5 (23.7 to 25.1). Audit 2 launched on the replay, the driver and the plan.
- 2026-09-16 08:38. Audit 2 returned: launch with fixes. Fixed: prose numbers that did not match the tables (the paint rule removes 42 to 75 percent of fragments; the person rule's regressions; SS03-19 under paint; the sensitivity examples; "position 0" is position 1 of 66); the axis-stage reading, which had called the Amateur-3 change a recovery and had stated two unmeasured explanations; the saturation claim (the 90 cut-off removes about a quarter of the marking fragments on the two Amateur-2 views by saturation alone, since their marking saturation p75 is 95 and 96; 90 was a round number near GX0's marking p75 of 85, and nothing else recommends it); the design-history wording (above); the input writer, whose gzip header carried the current time so identical inputs hashed differently (now a fixed header time and an atomic replace); the estimator records now carry the kept fragment indices and the accounting gate checks them, and re-derives own-direction arms' directions; the observation-only arm is named `paint_observations` at both stages and a `person_observations` counterpart is added; the dead `ideal_markings_supported` column is dropped; the remote import check is recorded in the cruft folder; tests cover the vanishing-point angles and the fragment masks. Decision on arms: four matcher arms, `paint_observations`, `person_observations`, `person` and `paint`; the last is kept because its axis-stage prediction is poor on four views and the matcher run tests whether the local replay predicts the matcher, which every conclusion here leans on. Noted, not changed: `run_matcher.py` has no resume guard; code hashes cover this folder only; person boxes come from every sampled frame in the pack, so a fragment is masked wherever any player stood in the three-second window.
