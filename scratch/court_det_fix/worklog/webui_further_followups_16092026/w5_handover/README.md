# W5 handover — directional court admission

## Bottom line

The next remote experiment is a three-arm directional visibility sweep over the
fixed 27-case development panel: `(3,3)`, `(4,3)` and `(5,3)`. Do not run
`(0,0)`. A fresh session must first land the small self-contained W5 dispatch
repair recorded in the active campaign `RESUME.md`.

G1 r3 was active when this session closed. Its last report had six workers on
distinct cases, eleven queued cases, no failures and no r3 promotions. The
visible Carmack worker owns that run.

## Fresh-session startup

Read only:

1. repository instructions;
2. `local_scratch/campaigns/w5-line-admission/RESUME.md`;
3. `local_scratch/campaigns/w5-line-admission/plan.md`;
4. `local_scratch/campaigns/w5-line-admission/PROJECT_STATE.md`;
5. `local_scratch/campaigns/w5-line-admission/w5_launch_packet.md`.

`W5_GO.md` permits launch after G1 and the W5 code/data gates pass. It does not
permit the known-broken `51cd6bb` W5 checkout.

## Exact implementation task

Make one direct patch and review it once:

- compare only the three retained arms;
- save full internally computed floor-zero proposal IDs in every arm;
- fix provenance-aware gallery and G0 replay calls;
- freeze the missing camera helper;
- make preflight open pilot controls;
- use a pushed Git commit for tracked code and one receipt for ignored data.

Run focused tests, Ruff, whole-project Pyrefly, Bash syntax and ShellCheck.
Commit as `Make W5 dispatch self-contained`, push, and let the visible worker
follow the standing dispatch. Do not start another broad design or provenance
audit.

## After remote results return

Build the compact three-arm comparison. Inspect every wrong/unclear view,
prior-ruling conflict and regression case excluded from contact sheets at
full-frame scale. Choose one global floor only if the evidence supports it;
otherwise reject the rule.

Update the line-template result pointer, steering record and this handover. Run
Claude Opus 5 High on the W5 result and final historical-impact accounting. The
user has already approved sharing the necessary project evidence with
Anthropic.

## Preserved follow-up

After W5 interpretation, run the queued court-detector tidy. Inventory the
court-detector tree and related `docs/` and `experiments/` artefacts, map
references, take a `local_scratch` tarball, agree the disposition list with the
user, then move or delete. Preserve worklogs and finish with one canonical
entry document.

The current project-quality and deployment account is in
`local_scratch/campaigns/w5-line-admission/PROJECT_STATE.md`.
