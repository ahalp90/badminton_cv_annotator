GT-JOIN SCORING: sticky vs pool landing kinematics against ShuttleSet GT (pilot)

AUTHORITATIVE (gt_scoring.score_video primary landing counts, denominator = all GT rallies):
  STICKY arm (patched chain):  landing-correct 22 / 113
  POOL   arm (hybrid: patched chain + pool landing kinematics): landing-correct 42 / 113

RECONCILIATION vs the recorded capture:
  capture scored the patched (sticky) chain at 22/113.
  this run scored STICKY at 22/113. match: True
  gt_scoring reference (pre-patch, pool-era) landing_primary_correct = 45.
  this run scored POOL hybrid at 42/113.
  per-rally row-sum cross-check: sticky 22 (== agg 22: True), pool 42 (== agg 42: True).
  GT-half map arm-agnostic (sticky rows == pool rows): True

LOOP-ZONE (25 rallies) GT court-half mix and coin-flip count:
  matched to a GT landing: 25 of 25 (unmatched: [])
  GT half (matched): TOP 15, BOT 10
  pool arm scored correct over the loop zone: 15 of 25
  sticky arm scored correct over the loop zone: 0 (sticky landings are all None here, so 0 by construction)

NON-LOOP FLIPPED (13 rallies) pool-arm correctness (genuine landings the sticky cap loses):
  rally ids: [9, 10, 11, 18, 28, 33, 44, 58, 59, 62, 66, 74, 104]
  matched to a GT landing: 12 of 13 (unmatched: [44])
  GT half (matched): TOP 6, BOT 6
  pool arm scored correct: 6 of 13 (6 of 12 matched)

SHARED-None (26 rallies, None under both arms) correctness (must be 0):
  rally ids: [3, 4, 14, 17, 22, 26, 30, 32, 37, 39, 46, 48, 53, 56, 68, 73, 75, 82, 84, 88, 93, 95, 96, 103, 105, 112]
  pool correct:   0
  sticky correct: 0

DEGENERATE POOL LANDING over the 38 flipped rallies (the key caveat):
  pool landing court-half distribution: {'Top': 38}
  GT half over the 37 matched flipped rallies: TOP 21, BOT 16
  pool arm scored correct over the flipped set: 21
  Every flipped-rally pool landing projects to the TOP half; not one lands BOT. The pool
  arm is not discriminating on these rallies, it always guesses Top, so its "correct"
  count is just the GT-Top tally. This is the far-baseline carry/pickup terminal the
  settle cap was built to suppress (point_winner module docstring: an ELEVATED terminal
  projects past the far baseline and lands the verdict in the wrong half). The sticky cap
  returning None here is the cap working: it trades landing recall for not shipping an
  always-Top guess. Over the loop zone that guess matches GT 15 of 25 (all 10 BOT wrong).

LOOP-FLAG RE-DERIVATION CROSS-CHECK:
  The supplied 25-list is used verbatim as the authoritative loop_zone_flag in the CSV,
  so there is no disagreement to report on the flag itself. The two-part criterion was
  probed against the pipeline outputs:
  (A) final_contact within [-2,+1] of a 16-frame lattice point (fc mod 16 in 14,15,0,1):
      loop-zone members satisfying (A): 25 of 25
      non-loop flipped satisfying (A):  13 of 13
      loop fc mod 16:     [1, 1, 1, 1, 1, 1, 1, 1, 14, 14, 14, 14, 14, 14, 14, 14, 14, 14, 14, 14, 14, 14, 14, 14, 14]
      non-loop fc mod 16: [14, 14, 14, 14, 14, 14, 14, 14, 14, 14, 14, 14, 14]
  Condition (A) holds for ALL 38 flipped rallies (every final contact sits at fc mod 16
  in {1, 14}, i.e. -2 or +1 off a 16-multiple): the flipped set is lattice-locked, which
  corroborates the "16-periodic zone" framing. (A) therefore does NOT separate the 25
  loop from the 13 non-loop; the separator is condition (B) "pool landing lies inside the
  zone", which needs Sol's zone-boundary construction (not emitted by run_video/gt_scoring).
  loop_flag_diag.csv carries rally_id, final_contact, pool_landing_frame for all 38.
